# Install the local package (core: pandas, networkx, numpy, scipy, scikit-learn, openai, requests)
-e .

# Environment variable management
python-dotenv>=1.0.0

# FastAPI backend
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.5

# Graph database client (Memgraph/Neo4j)
neo4j>=5.0.0
# PostgreSQL driver for the durable session store (e.g. Railway)
psycopg2-binary>=2.9.0

# Visualization (backend-side chart generation)
plotly>=5.18.0
matplotlib

# Quality Data Platform dependencies
tabpfn-client>=0.1.0
tabpfn-extensions[unsupervised]>=0.2.0
shap>=0.45.0,<0.51.0
rich<=14.0.0,>=13.7.0

# Dev / test
pytest
