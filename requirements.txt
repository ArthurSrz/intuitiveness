# Install the local package
-e .

# Environment variable management
python-dotenv>=1.0.0

streamlit_pdf_viewer
neo4j>=5.0.0
networkx
pandas
matplotlib
scipy
scikit-learn
# sentence-transformers removed - using HF Inference API instead (see models.py)
streamlit>=1.28.0
streamlit-extras>=0.7.0
streamlit-agraph>=0.0.45
plotly>=5.18.0
openai>=1.0.0
streamlit-javascript>=0.1.5
numpy
# Note: Using built-in st.json() for JSON visualization (streamlit-json-viewer doesn't exist on PyPI)
requests

# Quality Data Platform dependencies
tabpfn-client>=0.1.0
tabpfn-extensions[unsupervised]>=0.2.0
# SHAP with Python 3.11+ support (avoid old numba/llvmlite incompatibility)
shap>=0.45.0,<0.51.0
# Pin rich version to satisfy tabpfn-client requirement
rich<=14.0.0,>=13.7.0
 
